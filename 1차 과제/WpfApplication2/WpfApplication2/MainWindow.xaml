<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApplication2"
        Title="MainWindow" Height="160" Width="525">
    
    <Window.Resources>
        <local:CommaConverter x:Key="converter2"/>
    </Window.Resources>

        <StackPanel>
        <CheckBox x:Name="Check_Box" Margin="10" >Target TextBox ReadOnly</CheckBox>
        <TextBox x:Name="src_Box" Width="480" Height="30" Padding="5" Margin="0,0,0,10" Text="{Binding Str, Mode=TwoWay}"/>
        <TextBox x:Name="tar_Box" Width="480" Height="30" Padding="5" 
                 Text="{Binding ElementName=src_Box, Path=Text,Converter= {StaticResource converter2}}"
                 IsReadOnly="{Binding ElementName=Check_Box,Path=IsChecked, Mode=OneWay}"></TextBox>
    </StackPanel>
</Window>
